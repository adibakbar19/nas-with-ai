# Backend API base URL (no trailing slash).
# Leave empty to use same-origin /api requests via nginx proxy.
VITE_API_BASE_URL=

# Optional local docker host port for docker-compose.frontend.yml
FRONTEND_PORT=8088
