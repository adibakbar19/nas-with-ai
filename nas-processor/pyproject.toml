[project]
name = "nas-processor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["polars"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["nas_processor"]
